# SOLEN shell (zsh) — optional, guarded include
# Copy to ~/.config/solen/zshrc.solened and add to ~/.zshrc between:
#   # SOLEN: begin
#   source "$HOME/.config/solen/zshrc.solened"
#   # SOLEN: end

# History
HISTFILE=~/.zsh_history
HISTSIZE=10000
SAVEHIST=10000
setopt appendhistory HIST_IGNORE_ALL_DUPS HIST_REDUCE_BLANKS

# Prompt fallback
PROMPT='[%n@%m]%~%# '

