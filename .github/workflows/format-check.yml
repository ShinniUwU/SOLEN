name: Check Shell Script Formatting 🎨

on:
  pull_request:
    branches: [ main ]
<<<<<<< HEAD
    paths:
      - 'serverutils'
      - 'Scripts/**'
      - '.editorconfig'
  push:
    branches: [ main ]
    paths:
      - 'serverutils'
      - 'Scripts/**'
      - '.editorconfig'
=======
    paths-ignore:
      - 'docs/**'
      - '**/*.png'
      - '**/*.jpg'
      - '**/*.md'
  push:
    branches: [ main ]
    paths-ignore:
      - 'docs/**'
      - '**/*.png'
      - '**/*.jpg'
      - '**/*.md'
>>>>>>> 2366f52 (feat(motd,install,security,backups,ci): MOTD panels, cross-distro installer, backup verify, UFW+SSH rollback quiter CI)

permissions:
  contents: read

jobs:
  shfmt-check:
    name: Check Formatting (shfmt) ✨
    runs-on: ubuntu-latest
    continue-on-error: true  # don't block merges while stabilizing

    steps:
      - name: Check out code 🛒
        uses: actions/checkout@v4

      - name: Run shfmt Format Check 🧐
        run: |
          echo " "
          echo "🎨 Running 'shfmt -i 2 -ci -sr -d' on ./Scripts and ./serverutils ..."
          echo "-------------------------------------------------------------"
          docker pull mvdan/shfmt:latest-alpine
          docker run --rm -v "$(pwd):/mnt" -w /mnt mvdan/shfmt:latest-alpine -i 2 -ci -sr -d ./Scripts ./serverutils
          echo "-------------------------------------------------------------"
          echo "✅ Formatting check complete! (Will fail if diffs were found)"
